# Environment variables template
# Copy this to .env and fill with your actual values
# Never commit .env to version control!

# Django Settings
DJANGO_SECRET_KEY=seu-secret-key-super-seguro-e-aleatorio-aqui
DJANGO_DEBUG=False

# Server Configuration
ALLOWED_HOSTS=localhost,127.0.0.1,seu-dominio.elasticbeanstalk.com,seu-dominio.com

# Database (AWS RDS PostgreSQL)
# Format: postgres://user:password@host:port/dbname
DATABASE_URL=postgres://user:password@seu-rds-endpoint:5432/censo_amip_db

# CORS - Frontend origins allowed
CORS_ALLOWED_ORIGINS=https://seu-dominio.com,https://seu-dominio.elasticbeanstalk.com

# Django Settings Module
DJANGO_SETTINGS_MODULE=projeto.settings
